diff a/server/maps/index.js b/server/maps/index.js	(rejected hunks)
@@ -2,3 +2,3 @@ const router = require("express").Router();
-const {User, Product} = require("../db/models");
-require("../../secrets")
-const gMapsClient = require('@google/maps').createClient({
+const { User, Product } = require("../db/models");
+require("../../secrets");
+const gMapsClient = require("@google/maps").createClient({
@@ -6,2 +6 @@ const gMapsClient = require('@google/maps').createClient({
-})
-
+});
@@ -16,10 +14,15 @@ router.put("/sort", async (req, res, next) => {
-    gMapsClient.distanceMatrix({
-    origins: origins,
-    destinations: destinations
-  },  function(err, response) {
-    if (!err) {
-      console.log('this is response', response.json.rows[0].elements[0].distance.value)
-      res.json(response.json.rows[0].elements[0].distance.value)
-
-    }})
-
+    gMapsClient.distanceMatrix(
+      {
+        origins: origins,
+        destinations: destinations
+      },
+      function(err, response) {
+        if (!err) {
+          console.log(
+            "this is response",
+            response.json.rows[0].elements[0].distance.value
+          );
+          res.json(response.json.rows[0].elements[0].distance.value);
+        }
+      }
+    );
@@ -31,2 +34 @@ router.put("/sort", async (req, res, next) => {
-router.post("/reverse", async(req, res, next) => {
-
+router.post("/reverse", async (req, res, next) => {
@@ -35,7 +37,11 @@ router.post("/reverse", async(req, res, next) => {
-    gMapsClient.reverseGeocode({
-      latlng: latlng
-    },  function(err, response) {
-      if (!err) {
-        console.log('this is response', response.json)
-        res.json(response.json.results[0].formatted_address)
-      }})
+    gMapsClient.reverseGeocode(
+      {
+        latlng: latlng
+      },
+      function(err, response) {
+        if (!err) {
+          console.log("this is response", response.json);
+          res.json(response.json.results[0].formatted_address);
+        }
+      }
+    );
@@ -45,3 +51 @@ router.post("/reverse", async(req, res, next) => {
-})
-
-router.post("/geocode", async(req, res, next) => {
+});
@@ -51,8 +56,15 @@ router.post("/geocode", async(req, res, next) => {
-    gMapsClient.geocode({
-      address: address
-    },  function(err, response) {
-      if (!err) {
-        const latlng = [response.json.results[0].geometry.location.lng,response.json.results[0].geometry.location.lng]
-        console.log('this is response', response.json)
-        res.json(latlng)
-      }})
+    gMapsClient.geocode(
+      {
+        address: address
+      },
+      function(err, response) {
+        if (!err) {
+          const latlng = [
+            response.json.results[0].geometry.location.lng,
+            response.json.results[0].geometry.location.lng
+          ];
+          console.log("this is response", response.json);
+          res.json(latlng);
+        }
+      }
+    );
@@ -62 +74 @@ router.post("/geocode", async(req, res, next) => {
-})
+});
